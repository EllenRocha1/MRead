<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "MRead - Minha Estante Virtual" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%# Bootstrap CSS %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    
    <%# Bootstrap Icons %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-light">
    <%# 1. HEADER / NAVBAR (Requisito 1.1) %>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm mb-4">
      <div class="container">
        <%= link_to root_path, class: "navbar-brand fw-bold d-flex align-items-center" do %>
          <i class="bi bi-book-half me-2"></i> MRead
        <% end %>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto align-items-center">
            <li class="nav-item">
              <%= link_to "Biblioteca", root_path, class: "nav-link" %>
            </li>

            <% if user_signed_in? %>
              <%# Exibe o nome do usuário logado (Requisito 1.1) %>
              <li class="nav-item">
                <span class="nav-link text-info ms-lg-3">
                  <i class="bi bi-person-circle"></i> <%= current_user.email %>
                </span>
              </li>
              <li class="nav-item">
                <%= link_to "Novo Livro", new_book_path, class: "btn btn-primary btn-sm ms-lg-3" %>
              </li>
              <li class="nav-item">
                <%= button_to "Sair", destroy_user_session_path, method: :delete, class: "btn btn-link nav-link border-0" %>
              </li>
            <% else %>
              <%# Botões para visitantes (Requisito 1.1) %>
              <li class="nav-item ms-lg-3">
                <%= link_to "Login", new_user_session_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Criar Conta", new_user_registration_path, class: "btn btn-outline-primary btn-sm ms-lg-2" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <%# 2. MENSAGENS DE FLASH (Sucesso/Erro) %>
    <div class="container">
      <% if notice %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>
      <% if alert %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <%= alert %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>

      <%# 3. CONTEÚDO DA PÁGINA %>
      <main>
        <%= yield %>
      </main>
    </div>

    <%# Bootstrap JS Bundle%>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
  </body>
</html>